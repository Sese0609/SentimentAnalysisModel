# SentimentAnalysisModel
A binary sentiment classifier (positive/negative) built with scikit-learn that incorporates automatic spam and anomaly filtering using Isolation Forest before classification,a sentiment classifier using LogisticRegression and a TfidVectorizor. 

A confusion matrix is also used for validation data.



